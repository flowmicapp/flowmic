// verify/lint/i18n-add-locale-cost-baseline.mjs
//
// GENERATED by: node -e "import(...).collect()" — see i18n-add-locale-cost.mjs.
//
// 🔴 THIS NUMBER IS THE COST OF ADDING A LANGUAGE, and it exists to go DOWN.
// Each entry is a place in hand-written source that lists the product UI
// languages itself instead of deriving them from packages/protocol/src/locales.ts.
// Adding a tenth language means editing every one of them by hand.
//
// Pinned at 54 on 2026-08-14, the day the registry landed. The gate fails on
// anything NEW; retiring entries is the work of the migration (architecture doc §8),
// and a shrinking baseline is the only evidence that the property is real.
//
// ⚠️ Do not re-baseline to silence a failure. A new entry means someone hand-rolled
// a locale list, which is exactly what this gate exists to notice.
//
// 🔴 STILL 54 AFTER THE MOBILE MIGRATION (P1, 2026-08-14) — re-pinned, not reduced,
// and the reason is worth more than the number. P1 removed 678 four-language call
// sites from the Flutter catalogue, and this gate did not move, because it never
// counted them: `_t(zh: …, en: …, ja: …, ko: …)` spells its languages as bare
// argument NAMES, and none of them is one of this gate's tokens. What it counts is
// something else — hard-coded `AppLocale.*` arrays, mostly in tests.
//
// Two consequences, both of them the point of writing this down:
//   · P1's real result is measured elsewhere (i18n/mobile/coverage.json plus the
//     fact that adding a language now edits no Dart at all). Reading a flat number
//     here as 「the migration achieved nothing」 would be reading the wrong ruler;
//   · four class tokens were ADDED to the gate in the same commit, because 34 test
//     fixtures moved from `AppStrings(AppLocale.zh)` to `const AppStringsZh()`. Had
//     they not been added, this file would have shrunk to 53 — an improvement made
//     entirely of the gate no longer being able to see. Line numbers below shifted
//     for the same reason; the sites are the same sites.

// 🔴 54 -> 40 SITES / 34 -> 23 FILES (2026-08-14, desktop P2 + the mobile tree).
// The number went DOWN, which is the only direction this file is allowed to move
// on its own account, and the two halves of the drop are worth separating:
//
//   · THE DESKTOP MIGRATION retired FOUR entries —
//     `apps/desktop/src/lib/strings/locale.ts#1` (UI_LOCALES now comes from the
//     registry) plus three test files whose hand-listed four-locale arrays became
//     loops over UI_LOCALES (locale.test.ts, first-run-locale.test.ts,
//     portable-copy.test.ts). That is honest, and it is also
//     SMALLER THAN THE WORK: the 15 shards each carried four per-locale blocks
//     and the two pickers each carried a four-row `LANG_LABEL`, and this gate
//     never saw any of them. Its tokens are the QUOTED forms `'zh-CN'` / `'ja'` /
//     `'ko'`, while an object written `{ 'zh-CN': {…}, en: {…}, ja: {…} }` spells
//     three of the four as BARE KEYS — one distinct token in the window, under
//     MIN_DISTINCT. So 17 hand-rolled locale lists were deleted by this migration
//     and this ruler could see one of them. Reading the -1 as 「the migration
//     achieved almost nothing」 would be reading the wrong ruler — the same lesson
//     P1 wrote into the paragraph above. The desktop result is measured elsewhere:
//     i18n/desktop/coverage.json, and the fact that adding a language now edits no
//     .ts and no .vue at all.
//     ⚠️ The blind spot is NOT worth closing by adding bare `ja:` / `ko:` to
//     CODE_TOKENS: those are ordinary property names, and a gate that fires on
//     them would be re-pinned by the third person who met it. What removed those
//     17 lists is that there is no longer a place to write one.
//   · THE REST moved with the mobile catalogue work that is in this tree.
//     `apps/mobile/test/support/locale_terms.dart#2` is the only entry that is NEW
//     rather than smaller: it is a shared fixture that ABSORBED the enumerations
//     which vanished from eight test files also listed as retired. Recorded rather
//     than waved through — a new entry is what this gate exists to make someone
//     look at, and looking at it is what this paragraph is.

// 🔴 `data_flow_disclosure_test.dart` 10 -> 9 (2026-08-19, local-model onboarding
// batch) — RE-PINNED, AND THE NUMBER IS NOT AN IMPROVEMENT. Say it plainly,
// because a baseline that only ever records good news is the version of this
// file that stops being read.
//
// What happened: the desktop gained a button that downloads the speech model, so
// the disclosure line 「it downloads only if you set FLOWMIC_SHERPA_AUTO_DOWNLOAD=1」
// became false and was rewritten to promise CONSENT instead of naming a variable.
// Pinning that promise needs one marker per language. Written as its own nine-entry
// map it read 11 here — a NEW hand-rolled locale list, correctly flagged. So the
// two per-locale maps in that test were merged into ONE map of records
// (`({qualifier, consent})`), which is the honest fix: language ten now threads
// through one list in that file, exactly as it did before this batch.
//
// ⚠️ But the reading moved 10 -> 9, not 10 -> 10, and the extra unit is WINDOW
// ARITHMETIC, not a retired list: the record layout spreads `AppLocale.*` further
// than the 240-char window, so one site the gate used to see it no longer does.
// Measured, not assumed (`collect()` before and after the merge: 11 -> 9). Counting
// that -1 as progress would be the trap this file's own P1/P2 notes describe — an
// improvement made entirely of the ruler seeing less.
//
// 🔴 SAME BATCH, SAME CAVEAT, LOUDER: `data-flow-disclosure.test.ts#1` is REMOVED
// below, and NOT because a locale list was retired. That file still hand-lists all
// nine languages several times over — the same edit merely moved the quoted `'ja'`
// / `'ko'` / `'zh-CN'` tokens far enough apart that no 240-char window holds three
// of them any more, and the gate now reads that file as ZERO. Nothing there got
// cheaper. The entry is dropped so this ledger describes what the gate can
// actually see (a pin nobody re-reads is how the last guard in this repo rotted),
// but a future reader must not take the deletion as evidence: the desktop
// disclosure test is still a place a tenth language has to be threaded into by
// hand, and this ruler has gone blind to it.
export const SITES = [
  'apps/desktop/src/capsule/capsule-timeline-one-word.test.ts#1',
  'apps/desktop/src/main-window/cloud-signout-confirm.test.ts#2',
  'apps/desktop/src/main-window/credentials-at-rest-note.test.ts#1',
  'apps/desktop/src/main-window/polish-capability-notice.test.ts#1',
  'apps/desktop/src/main-window/scenario-inference-consent.test.ts#2',
  'apps/desktop/src/main-window/timeline-search.test.ts#2',
  'apps/mobile/lib/src/settings/app_strings.dart#1',
  'apps/mobile/test/capsule_taken_exit_test.dart#1',
  'apps/mobile/test/cloud_image_error_copy_test.dart#1',
  'apps/mobile/test/data_flow_disclosure_test.dart#9',
  'apps/mobile/test/delivery_refusal_note_test.dart#1',
  'apps/mobile/test/delivery_terminology_test.dart#2',
  'apps/mobile/test/diagnostics_engine_section_test.dart#1',
  'apps/mobile/test/i18n_four_locale_test.dart#1',
  'apps/mobile/test/outbox_pc_verdict_convergence_test.dart#1',
  'apps/mobile/test/p8_engine_status_wire_test.dart#1',
  'apps/mobile/test/pc_offline_note_test.dart#2',
  'apps/mobile/test/support/locale_terms.dart#2',
  'apps/mobile/test/wp5_rendered_copy_legibility_test.dart#2',
  'apps/server-core/src/stt/pool-config.ts#1',
  'apps/server-core/test/stt-pool-wiring.test.ts#2',
  'packages/protocol/test/restriction-reasons.test.ts#1',
];
